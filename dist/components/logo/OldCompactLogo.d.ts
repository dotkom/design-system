/// <reference types="react" />
interface Props {
    width?: string;
    primaryColor: string;
    secondaryColor: string;
}
declare const OldCompactLogo: ({ width, primaryColor, secondaryColor, }: Props) => JSX.Element;
export default OldCompactLogo;
