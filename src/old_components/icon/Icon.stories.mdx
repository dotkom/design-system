import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Icon from './Icon';
import Button from '../button/Button';
import styled from 'styled-components';
import { Paragraph } from '../../';

<Meta title="Global|Icon" component={Icon} />

export const ExampleIconContainer = styled.div`
  & > * + * {
    margin-top: 0.5rem;
  }
`;

# Icon

This icon component uses the Material icon font from Google, specifically in the outlined style.

You can find all the different icons [here](https://material.io/resources/icons/?style=outline).

When you follow the link above, you can find an icon you like, then enter the name of the icon into this component. The icons inherit color and font-size from its parents, allowing you to easily style the icons.

PS: for the icons to work you must remember to inlcude the fonts in your document head.

<Preview>
  <Story name="All">
    <ExampleIconContainer>
      <Paragraph style={{ fontSize: '24px' }}>
        Icons inherit
        <Icon name="whatshot" />
        font size
      </Paragraph>
      <Paragraph style={{ color: 'green' }}>
        <Icon name="lock" />
        Icons also inherit color
      </Paragraph>
      <Button>
        <Icon name="favorite" /> Lorem
      </Button>
      <br />
      <Button color="danger" variant="outline">
        <Icon name="warning" /> Ipsum
      </Button>
    </ExampleIconContainer>
  </Story>
</Preview>

<Props of={Icon} />
