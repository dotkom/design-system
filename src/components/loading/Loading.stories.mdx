import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import { colors } from 'common/colors';
import Spinner from './Spinner';

<Meta title="Feedback|Spinner" component={Spinner} />

# Spinner/loading component

<Preview>
  <Story name="All">
    <>
      <h3>Regular small spinner</h3>
      <Spinner />
      <h3>Colored small spinner</h3>
      <Spinner primaryColor={colors.primaryDark} secondaryColor={colors.hotpink} />
    </>
  </Story>
</Preview>

<Props of={Spinner} />
