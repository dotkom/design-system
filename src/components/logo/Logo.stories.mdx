import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Logo from './Logo';
import CompactLogo from './CompactLogo';
import ModernLogo from './ModernLogo';
import StaticSpinnerLogo from './StaticSpinnerLogo';
import { colors } from 'common/colors';
import { ExampleContainer } from 'common/containers';

<Meta title="Logo" component={ModernLogo} />

# Logo

The logo of Online, linjeforeningen for informatikk ved NTNU Trondheim.  
This is the official logo, meant to be used in its full form.

<Preview>
  <Story name="Logo">
    <ExampleContainer>
      <Logo width="400px" />
      <Logo width={500} primaryColor={colors.error} secondaryColor="#0A94C2" />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={Logo} />

# Compact Logo

This is the compact version of the logo, showing only the O with the lightning.
This can be used in places where the larger logo cannot fit, as a favicon, or application logo.

<Preview>
  <Story name="Compact Logo">
    <ExampleContainer>
      <CompactLogo width="150px" />
      <CompactLogo width={200} primaryColor={colors.error} secondaryColor="#0A94C2" />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={CompactLogo} />

# Modern Logo

**NOT APPROVED FOR OFFICIAL USE**

<Preview>
  <Story name="Modern Logo">
    <ExampleContainer>
      <ModernLogo width="150px" />
      <ModernLogo width={200} primaryColor={colors.error} secondaryColor="#0A94C2" />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={ModernLogo} />

# Futuristic Logo (Static Spinner)

**NOT APPROVED FOR OFFICIAL USE**

<Preview>
  <Story name="Static Spinner Logo">
    <ExampleContainer>
      <StaticSpinnerLogo width="150px" />
      <StaticSpinnerLogo width={200} primaryColor={colors.error} secondaryColor="#0A94C2" />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={StaticSpinnerLogo} />
