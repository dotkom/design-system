import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { colors } from 'common/colors';
import Spinner from './Spinner';
import SpinnerLogo from './SpinnerLogo';
import SpinnerCircles from './SpinnerCircles';
import { ExampleContainer } from 'common/containers';

<Meta title="Feedback|Spinner" component={Spinner} />

# Chasing Spinner

Online does not have an official spinner, which leaves you free to use whatever you want. In general though, it's wise to use the same spinner for the whole application. 

<Preview>
  <Story name="Chasing Spinner">
    <ExampleContainer>
      <Spinner />
      <Spinner primaryColor={colors.error} secondaryColor="#0FF0AE" />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={Spinner} />

# Spinner Circles

<Preview>
  <Story name="Spinner Circles">
    <ExampleContainer>
      <SpinnerCircles />
      <SpinnerCircles size={'4rem'} primaryColor={colors.primary} secondaryColor={colors.secondary} />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={SpinnerCircles} />

# Spinner Logo

Like mentioned in the logo variations, this isn't an official logo or anything.

<Preview>
  <Story name="Spinner Logo">
    <ExampleContainer>
      <SpinnerLogo />
      <SpinnerLogo size={'150px'} primaryColor={'#db7093'} secondaryColor={'#2f9c0a'} />
    </ExampleContainer>
  </Story>
</Preview>

<Props of={SpinnerLogo} />
